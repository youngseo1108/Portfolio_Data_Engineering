docker compose exec -T app pip install awscli

docker compose exec -T app bash -lc "
  export AWS_ACCESS_KEY_ID=admin \
         AWS_SECRET_ACCESS_KEY=supersecret \
         AWS_DEFAULT_REGION=us-east-1 && \
  aws --endpoint-url http://minio:9000 s3 mb s3://raw || true && \
  aws --endpoint-url http://minio:9000 s3 cp /work/data/raw/yellow_tripdata_2025-01.parquet s3://raw/yellow_tripdata_2025-01.parquet && \
  aws --endpoint-url http://minio:9000 s3 ls s3://raw/
"